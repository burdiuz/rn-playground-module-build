module.exports = () => ({
	'react-native-elements': (pkg) => pkg,
	deepmerge: (pkg) => pkg.$deepmerge,
	'react-native-ratings': (pkg) => pkg.$ratings,
	'react-native-status-bar-height': (pkg) => pkg.$statusBarHeight,
});
